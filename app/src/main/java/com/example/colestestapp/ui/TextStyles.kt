package com.example.colestestapp.ui

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

val currentTextStyle =
    TextStyle(color = Color.White, fontSize = 30.sp, fontWeight = FontWeight(600))
val redHeadline = TextStyle(color = Color.Red, fontSize = 15.sp, fontWeight = FontWeight(600))
val descriptionBlack =
    TextStyle(color = Color.DarkGray, fontSize = 12.sp, fontWeight = FontWeight(400))

val portraitHeaderTextStyle =
    TextStyle(color = Color.Black, fontSize = 50.sp, fontWeight = FontWeight(900))
val portraitDescriptionTextStyle =
    TextStyle(color = Color.DarkGray, fontSize = 20.sp, fontWeight = FontWeight(500))
val portraitIngredientTextStyle =
    TextStyle(Color.Black, fontSize = 20.sp, fontWeight = FontWeight(700))
val ingredientBulletRowTextStyle =
    TextStyle(color = Color.DarkGray, fontSize = 12.sp, fontWeight = FontWeight(500))